# PR Triage — deventerprisesds/journey-voice — 2026-09-21

Read-only triage pass. For each PR: what it changes, whether content already landed on origin/main
under a different commit (content-grep, not ancestor-check, since history has been squash-merged/rewritten),
merge-conflict status, staleness, and a recommendation.

## PR #13 — feat: app-load caching (persist React Query cache, stop reloading on bg/fg)
Head: `claude/app-load-caching-ieosfy` · Created 2026-06-30 · Base sha at creation: `f92f17a3ca8d650844fcfe4e3f2da03b50be92b2` · 2 commits, +640/-621 across 7 files (per GitHub PR diff).

**(1) What it actually changes** (read via `pull_request_read get_files`, patches inspected):
- `src/App.tsx`: swaps `QueryClientProvider` for `PersistQueryClientProvider` + `createSyncStoragePersister` (localStorage key `jv-rq-cache-v1`), sets `refetchOnWindowFocus:false`, `gcTime:24h`, `staleTime:5min` globally.
- `src/hooks/useUnifiedTasks.ts`: converts from raw `useEffect`/`useState` + direct Supabase calls to `useQuery`; realtime subscription now calls `queryClient.invalidateQueries` instead of refetching directly.
- `src/hooks/useAssistants.ts` (new) and `src/hooks/useChatHistory.ts` (new): extract two fetch waterfalls out of `CommsConsoleContext` into React Query hooks.
- `src/contexts/CommsConsoleContext.tsx`: removes ~130 lines of manual fetch/gate logic, wires in the two new hooks.
- `package.json`/`package-lock.json`: adds `@tanstack/react-query-persist-client` + `@tanstack/query-sync-storage-persister`.

**(2) Already on origin/main under another commit?** NO — checked by content, not just history:
- `git show origin/main:src/hooks/useAssistants.ts` and `useChatHistory.ts` → **both `fatal: does not exist`**.
- `git show origin/main:src/App.tsx` → still plain `QueryClientProvider` (grepped for `persist`/`QueryClientProvider`/`refetchOnWindowFocus`/`gcTime`) — no persistence wired in.
- `git show origin/main:src/hooks/useUnifiedTasks.ts` → still the raw `useEffect`/`useState` implementation, not React Query.
This feature has not landed on main in any form. Background/foreground reload behavior is presumably still the reported problem today.

**(3) Merge conflict?** YES, real conflicts — but only detectable properly by using the PR's actual historical base commit, not `git diff origin/main...origin/<branch>` (three-dot), because **this branch has no common ancestor with current origin/main at all** (`git merge-base` returns nothing; root commits differ entirely — `9a4f3f3b…` for main vs `47f98ece…` for the branch). This is the history-rewrite the task description warned about, confirmed directly: main's entire commit graph was replaced at some point after this PR was opened.
- A naive `git merge --allow-unrelated-histories` floods with dozens of spurious "add/add" conflicts across the whole repo (supabase functions, migrations, etc.) — this is an artifact of the unrelated-history merge, not a real signal.
- The GitHub PR object still records the real original base sha (`f92f17a3ca8d650844fcfe4e3f2da03b50be92b2`), which still exists as a commit object in the local clone (`git cat-file -t` → `commit`, just unreachable from current main). Using that as the true 3-way merge base (`git merge-file` per touched file, base/main/branch) gives the honest answer:
  - `src/App.tsx`: 0 conflicts (main only added 2 lines since base).
  - `package.json`: 0 conflicts.
  - `src/contexts/CommsConsoleContext.tsx`: **1 real conflict** (main changed this file substantially since the PR's base — 169 insertions/27 deletions — overlapping the PR's own ~130-line removal).
  - `src/hooks/useUnifiedTasks.ts`: **2 real conflicts** (main changed 34/19 lines since base; PR rewrites the whole hook to React Query).

**(4) Staleness:** Created 2026-06-30, ~83 days old, never updated. The two files with real conflicts (`CommsConsoleContext.tsx`, `useUnifiedTasks.ts`) are exactly the ones that have kept evolving on main since — so even a manually-resolved merge would likely be fighting current logic rather than cleanly layering on top of it. `useUnifiedTasks.ts` on main today still has the same shape as base (`useEffect`+`useState`, `withTimeout` wrapper, etc.) so the PR's rewrite is conceptually still applicable, but it would need to be re-derived against main's current version rather than merged mechanically.

**(5) Recommendation: NEEDS-HUMAN-CALL.**
The underlying feature (persist RQ cache, stop full reloads on backgrounding, extract fetch waterfalls into RQ hooks) is real, valuable, and genuinely un-landed — not a candidate for CLOSE-SUPERSEDED. But it is not a clean MERGE either: real conflicts exist in the two most substantive files, which have continued to change on main for ~3 months. A human/engineer should decide whether to re-implement the caching approach fresh against current `CommsConsoleContext.tsx`/`useUnifiedTasks.ts` (probably faster and safer than resolving the stale conflicts) or manually resolve this PR's conflicts. Not CLOSE-STALE outright because the problem it solves is still open on main.

## PR #16 — Fix test custom alarm sound button to play stored alarm sound
Head: `claude/update-claude-md-docs-da8gc4` · Created 2026-07-22, never updated · Base sha: `e2baeb1a7423403ba9c77e70fc8c76f4199967ac` · 1 commit, +17/-10, 1 file.

**(1) What it actually changes** (title is misleading — despite the branch name mentioning "claude-md-docs", the actual diff is a bug fix, not doc work):
`src/components/NotificationSettings.tsx` — `sendTestCustomAlarm()`: if `window.AndroidBridge.testAlarmSound` exists, call it directly (plays the user's stored alarm sound immediately, bypassing the notification-shade gate flow that previously meant no sound played during the test). Falls back to the old `notify()`-based path for older APKs that lack `testAlarmSound`.

**(2) Already on main?** NO. `git show origin/main:src/components/NotificationSettings.tsx | grep sendTestCustomAlarm` shows the function is still the **old, unfixed** version (plain `notify()` call, no `testAlarmSound` branch). `git grep testAlarmSound origin/main` returns zero hits anywhere in the repo — this bridge-method branch was never added on main another way.

**(3) Merge conflict?** NONE — and unusually clean evidence for it: `git diff <PR base sha> origin/main -- src/components/NotificationSettings.tsx` is **empty** — this specific file is byte-identical between the PR's original base commit and current origin/main (confirmed with `diff -q`, no output). So even though this branch also predates the repo's history rewrite (no `git merge-base` with current main), the one file it touches was untouched by that rewrite's other changes. A real 3-way `git merge-file` (base/main/PR) applies clean, exit 0, 0 conflict markers.

**(4) Staleness:** ~61 days old, but not meaningfully stale — the target file hasn't moved on main at all since the PR's base, so there's nothing for it to have drifted against.

**(5) Recommendation: MERGE.**
Small, single-file, additive (keeps the old fallback path intact so nothing regresses on older APKs), the file is unchanged on main since the PR's base so it applies cleanly, and the bug it fixes (test button not playing the custom sound) is still present on main today. Lowest-risk PR of the five.

## PR #17 — "Scheduler: assessment + trait model + plan (docs/memory)"
Head: `claude/mobile-widget-web-bridge-debug-nsishi` · Created 2026-07-24, last updated 2026-07-31 (7 days of activity) · Base sha: `e2baeb1a7423403ba9c77e70fc8c76f4199967ac` (same base as PR #16) · 23 commits, +1605/-194 across 14 files.

**(1) What it actually changes — the PR title/description is STALE relative to its own current diff.** The description says "Design/assessment only — **no scheduler code changed**" and "This branch will accumulate the scheduler implementation once acceptance criteria are signed off" — but `pull_request_read get_files` shows the branch grew well past that first docs-only commit (`7eae8c2 docs(scheduler): persist heavy assessment...`) into 22 more commits of REAL implementation:
- `.claude/memory.md` (new, 289 lines) + `CLAUDE.md` (+40) — the original assessment/plan.
- **Real code changes**, per commit log (`de-fork smart-calendar-scheduler`, `after_work 17-19 reconcile`, `revive dead GUI settings`, `systematic trait layer`, `weekend-evening protection`, `pin booked appointments`, `maxDailyHours daily capacity guard`, `value-aware overflow queue`, `external-meeting confirmation + slot release`, `B4 default-cap regression fix`, `config-authoritative placement rule`) touching: `src/config/schedulingRules.ts`, `src/utils/buildDayContext.ts`, `src/components/DailyReviewModal.tsx`, `supabase/functions/_shared/{build-day-context,scheduling-defaults}.ts`, `supabase/functions/{batch-calendar-scheduler,execute-tool,nightly-schedule-builder,smart-calendar-scheduler}/index.ts`.
- Two new migrations: `task_overflow_queue.sql`, `external_event_attendance.sql`; one new edge function: `confirm-external-meeting/index.ts`.

**(2) Already on main under another commit?** Checked by content, NOT by title:
- `confirm-external-meeting/index.ts`, both new migrations → `fatal: does not exist` on origin/main. Not landed.
- `scheduling-defaults.ts` on main still has `after_work: { start: 17, end: 22, ... }` — the exact drift the PR says it fixes to 17-19 is **still present, unfixed, on main today**.
- `git grep overflow_queue\|overflowQueue` on origin/main → zero hits. Value-aware overflow queue not landed.
- Corroboration from a second source: huddle-extension-app's own `CLAUDE.md` backlog section (dated after this PR) still lists *"fix the `after_work` window drift (17–22 in scheduling-defaults vs 17–19 in ... `find_open_slots`)"* as an **open, undone** item — independent confirmation this specific fix never landed anywhere.
- So: none of this PR's actual scheduler fixes have landed on main by another route.

**(3) Merge conflict?** YES, real conflicts, checked via true 3-way `git merge-file` against the actual base commit (again no `git merge-base` exists with current main — pre-rewrite branch):
| file | conflicts | base vs current-main |
|---|---|---|
| `src/config/schedulingRules.ts` | **1** | differs |
| `supabase/functions/execute-tool/index.ts` | **1** | differs — **+337/-61 lines since base** |
| `supabase/functions/nightly-schedule-builder/index.ts` | **5** | differs |
| `src/components/DailyReviewModal.tsx` | 0 | unchanged |
| `src/utils/buildDayContext.ts` | 0 | unchanged |
| `supabase/functions/_shared/build-day-context.ts` | 0 | unchanged |
| `supabase/functions/_shared/scheduling-defaults.ts` | 0 | differs, but non-overlapping lines |
| `supabase/functions/batch-calendar-scheduler/index.ts` | 0 | unchanged |
| `supabase/functions/smart-calendar-scheduler/index.ts` | 0 | unchanged |
| `CLAUDE.md` | 0 | differs, non-overlapping |

**(4) Staleness — this is the decisive factor.** `nightly-schedule-builder/index.ts` and `execute-tool/index.ts` are exactly the files this PR most heavily rewrites, and both have kept evolving **independently** on main since: `git log <base>..origin/main -- nightly-schedule-builder/index.ts` shows 6 separate later commits (caveat-overlay read-time relaxation, WIP-status preservation fix, parking-lot exclusion, an unrelated merge) that this PR's branch knows nothing about. `execute-tool/index.ts` gained +337/-61 lines independently. Scheduling is clearly this codebase's most actively-worked area (per journey-voice/huddle-extension-app CLAUDE.md's extensive scheduler notes) — a 2-month-old branch touching its two hottest files is very likely to be reasoning about a version of the logic that no longer matches current architecture, even where a merge is mechanically resolvable.

**(5) Recommendation: NEEDS-HUMAN-CALL.**
The underlying analysis (drifted `after_work` window, array-vs-string bug, path-dependent smart-calendar-scheduler) is well-evidenced and at least one finding (`after_work` drift) is independently confirmed STILL UNFIXED on main today — so this isn't simply obsolete. But the two core files it rewrites have been actively, independently developed on main for two months since, producing real conflicts. A mechanical merge is not safe, and even a manually-resolved merge is unlikely to be semantically correct against the current nightly-schedule-builder logic. A human/engineer should review the `.claude/memory.md` assessment this PR persisted (still a useful design doc) and decide whether to re-derive the trait-model implementation fresh against current scheduler code, rather than merge this branch as-is. Not a clean CLOSE-STALE either, since the diagnosed problems appear to still be open.

## PR #21 — Add definition_of_done for the Huddle confirm-intent gate
Head: `claude/new-session-eonf2r` · Created 2026-07-29, updated 2026-07-31 (~53 days stale) · Base sha: `b805c94a6f8e4c25dae75ccf1249469c3416717a` · 10 commits, +332/-11 across 7 files.

**(1) What it actually changes** (full file contents read via `pull_request_read get_files`):
- `supabase/migrations/20260729000000_add_definition_of_done.sql` (new): additive `ALTER TABLE public.tasks ADD COLUMN IF NOT EXISTS definition_of_done TEXT`.
- `supabase/functions/execute-tool/index.ts`: maps `args.definition_of_done` into `updateData`/`data` in `updateTask`/`batchUpdateTasks`.
- `supabase/functions/_shared/tool-definitions.ts`: adds a `definition_of_done` property + description to the `update_task` tool's JSON schema (so the LLM knows the parameter exists).
- `.claude/actions.md`, `.claude/memory.md` (both new), `.claude/skills/cleanup-board/SKILL.md` (new), `CLAUDE.md` (+29 lines: `apply-migration.yml` escape-hatch doc + "Test-task naming convention").
- PR body claims the migration was **already applied and verified live** against Supabase (`information_schema.columns` check) independent of this PR merging.

**(2) Already on origin/main under a different commit? YES for the core feature — confirmed by content, this is the clearest supersession case of the five.**
- `git log origin/main -S "definition_of_done" -- supabase/functions/execute-tool/index.ts` → **`54f12c7 fix(execute-tool): persist definition_of_done on update_task/batch_update_tasks (#24)`**, confirmed a real ancestor of origin/main (`git merge-base --is-ancestor 54f12c7 origin/main` → yes). Reading that commit directly (ground truth, not a comparison of proxies): it maps `definition_of_done` in both `updateTask` and `batchUpdateTasks` — the same functional gap PR #21 closes — and its own commit message states explicitly: *"The public.tasks and mirror tasks.journey_tasks definition_of_done columns already exist."* That is independent, ground-truth confirmation that the DB column PR #21's migration would add is **already live**, matching PR #21's own live-verification claim, just landed through a separate PR (#24) with a cleaner implementation (also adds an empty-update-guard PR #21 lacks).
- `.claude/actions.md` and `.claude/memory.md`: main already has its own, independently-evolved versions of both (`memory.md` last updated **2026-09-13**, vs. PR #21's frozen 2026-07-29 snapshot) — these would be **add/add conflicts** if merged (confirmed by `git show origin/main:.claude/{actions,memory}.md` both existing with different content), not additive.
- **NOT landed anywhere:** the `definition_of_done` entry in `tool-definitions.ts`'s `update_task` schema (main's schema still lacks it — minor doc-for-the-LLM gap only, the underlying write-path works regardless since Huddle calls it directly), the `cleanup-board` skill, and the CLAUDE.md `apply-migration.yml`/`Test-task naming` doc additions.

**(3) Merge conflict?** Mixed, checked via real 3-way `git merge-file` against the PR's actual base commit:
- `CLAUDE.md`: 0 conflicts (clean).
- `supabase/functions/_shared/tool-definitions.ts`: 0 conflicts (clean — genuinely unlanded, could be added standalone).
- `supabase/functions/execute-tool/index.ts`: **1 real conflict** — PR #21 and main's PR #24 both edited the same `updateTask` region to add the identical `definition_of_done` mapping, independently.
- `.claude/actions.md`, `.claude/memory.md`: would be **add/add conflicts** (both sides created these paths independently with divergent content).
- Migration file, `cleanup-board/SKILL.md`: new paths on both sides — no conflict, but the migration is now a no-op re-add of an already-live column (harmless, `IF NOT EXISTS`) and low value.

**(4) Staleness:** ~53 days old. The core feature's code (execute-tool wiring) has been independently rewritten and merged as a cleaner fix (#24) with an added safety guard PR #21 doesn't have. The project docs it touches (`memory.md`, `actions.md`) have moved on substantially (6+ weeks of further updates on main).

**(5) Recommendation: CLOSE-SUPERSEDED.**
The PR's actual purpose — wire `definition_of_done` through `update_task`/`batch_update_tasks` and confirm the DB column — is already live on main via PR #24, verified by reading #24's own commit content (not just comparing titles). The doc files it adds conflict with main's own further-evolved versions. The two small pieces that genuinely never landed (the `tool-definitions.ts` schema entry so the LLM tool description mentions the field, and the `cleanup-board` skill) are minor, low-effort, and better applied as fresh small edits than by resurrecting this whole stale multi-purpose branch.

## PR #26 — "feat(scheduling): dryRun harness for nightly-schedule-builder (faithful, zero-write)"
Head: `claude/huddle-journey-integration-xokgv1` · Created 2026-08-11, most recently updated **2026-09-14** (7 days old, actively maintained) · Base sha: `724ff0a599b014385baebd07ddd0840670cf3705` · 204 commits (per GitHub), +29513/-797 across **97 files**, 4 PR comments · `mergeable_state: "clean"` (GitHub).

**(1) What it actually changes — the title/description dramatically understates scope; this is the single biggest gap between title and diff of the five PRs.** The PR body describes only a `dryRun` mode for `nightly-schedule-builder` (a faithful, zero-write test harness for the real scheduling pipeline). The actual diff includes that, but also a large amount of additional, unrelated-looking work accumulated on this long-lived branch: a Slack↔n8n integration (`cloudflare/src/slack-events.ts` +445, `notify.ts` +478, n8n workflow JSON exports, `docs/slack/ROLLBACK-event-subscriptions.md`), a `nexus.ts` assignment-matching module (+725 lines) and `nexusAssignments.ts`, `nudges.ts` (+405), `notification-body.ts` (+121), `task-dedup.ts` (+388), a `scripts/shadow-run/` SQL harness, `scripts/undef-check.mjs` (+553, a static-analysis guard), new CI workflows (`check-edge-deploy-drift.yml`, `checks.yml`), and its own copies of `.claude/memory.md` (+1562), `.claude/actions.md` (+1955), `.claude/accuracy-log.md` (+349) — all far beyond "a dryRun harness." Per the task instructions ("read the diff, not just the title"), this PR needs to be evaluated as the ~30k-line integration branch it actually is, not as its title suggests.

**(2) Already on main under a different commit? Not applicable in the usual sense** — verified `git merge-base origin/main origin/claude/huddle-journey-integration-xokgv1` returns `724ff0a599b014385baebd07ddd0840670cf3705`, which is **exactly current `origin/main` HEAD** (`git log --oneline -1 origin/main` matches). Since the branch's merge-base with main is current main's own tip, main cannot have gained any of this content through a separate later merge — by construction nothing here can have already landed elsewhere. (A raw `git log origin/main..branch` misleadingly shows 2513 commits including ancient pre-rewrite Lovable history reachable only from this branch's own old lineage — an artifact of git log's one-sided reachability, not a real signal; the tree-level diff via the correct merge-base is the reliable 97-file/29.5k-line figure above and is what matters.)

**(3) Merge conflict?** **NONE**, confirmed two ways: GitHub reports `mergeable_state: "clean"`, and an actual local `git merge --no-commit --no-ff origin/claude/huddle-journey-integration-xokgv1` onto a fresh `origin/main` worktree completed with **"Automatic merge went well"** and 0 files in conflict state.

**(4) Staleness:** Not stale — most recently updated 7 days ago (2026-09-14), and the branch has been actively, continuously rebased/merged against `origin/main` throughout its life (commit `514ec55 Merge origin/main into claude/huddle-journey-integration-xokgv1 (trait + caveats UNION)` and a later comment thread from 2026-09-13 shows the author deliberately resolving a 12-file conflict against main's scheduling-caveats work as a **union of both features**, not a one-sided overwrite — real, careful integration work, not neglect). PR comments (all from the repo owner) show active firefighting of CI issues (a 26 MB `mcp` function hitting Supabase's request-size limit on `workflow_dispatch: all` deploys — explicitly diagnosed as pre-existing and unrelated to this branch, not a regression it introduced) and course-correction (one comment is a self-correction of an earlier wrong root-cause).

**Caveats that keep this from being a simple MERGE despite the clean/fresh signals:**
- `get_status` on the head SHA returns **`"state":"pending","total_count":0`** — no CI check results are attached to the final commit; the "clean"/tested claims live in the author's own PR comments, not in an independently-visible passing check run.
- The branch's own recent commit messages (e.g. `fix(notify): scheduled-call emails carry the real briefing, not one sentence`, `fix(slack): a DM pins the configured agent instead of fanning out`, `docs(actions): the 8am email 401 — root cause, the workflow defect I introduced`) describe the author finding and fixing real defects **within this same branch**, i.e. it reads as still-active in-flight development rather than a finished, reviewable unit.
- At 97 files / ~29.5k lines / 204 commits covering at least five distinct feature areas (scheduling dryRun+redesign, Slack/n8n integration, notification-body rewrite, task-dedup, nudges), this is too large and too broad for a single go/no-go call without the owner's own review of what's actually being shipped, especially since the title promises only a narrow scheduling harness.

**(5) Recommendation: NEEDS-HUMAN-CALL.**
Not for conflict or staleness reasons — both check out clean and current, better than any of the other four PRs — but because (a) the actual scope is far larger and more varied than the title states, spanning several independent feature areas that a reviewer would reasonably want to see as separate PRs; (b) there is no independently-verified CI status on the head commit, only the author's own narrated verification in PR comments; and (c) recent commits suggest the branch may still be mid-development rather than finished. The owner should decide whether to merge as one large PR, ask for it to be split by feature area, or wait for CI/independent verification before merging — not something to auto-classify as a safe MERGE or dismiss as stale/superseded.

---

## Summary

| PR | Recommendation | Why |
|---|---|---|
| #13 | NEEDS-HUMAN-CALL | Valuable, un-landed app-load-caching feature, but real 3-way merge conflicts in `CommsConsoleContext.tsx`/`useUnifiedTasks.ts`, which have kept evolving on main for ~3 months — a human should decide whether to reapply fresh vs. resolve. |
| #16 | MERGE | Small single-file bugfix, target file byte-identical to its base on current main, 0 conflicts, bug still present on main. |
| #17 | NEEDS-HUMAN-CALL | Description says "no scheduler code changed" but diff heavily rewrites `nightly-schedule-builder`/`execute-tool`, which have since been independently rewritten on main (6+ separate commits) — real conflicts, mechanical merge unlikely to be meaningful even though one diagnosed bug (`after_work` drift) is confirmed still open. |
| #21 | CLOSE-SUPERSEDED | Core feature (definition_of_done column + execute-tool wiring) already landed on main via a separate, independently-merged PR #24, confirmed by reading #24's own commit content; remaining doc files conflict with main's much-more-current versions. |
| #26 | NEEDS-HUMAN-CALL | Zero merge conflicts, fresh (updated 7 days ago), actively/carefully maintained against main — but its real diff (97 files, ~29.5k lines, 204 commits, 5+ distinct feature areas: Slack/n8n, notification rewrite, task-dedup, nudges, scheduling redesign) is far larger than its "dryRun harness" title suggests, and has no independently-visible passing CI check on its head commit — needs the owner's own scoping/review decision, not an auto-classification. |

