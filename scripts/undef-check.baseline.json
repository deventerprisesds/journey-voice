{
  "_what": "Known pre-existing undefined symbols, exempted from failing CI so that NEW ones can fail it. Keyed '<path>:<symbol>' — never by line number, which moves.",
  "_why": "scripts/undef-check.mjs was rewritten 2026-09-03 and found this on its first real run over the tree. It is a genuine latent defect, not a false positive, and it is NOT in the lane that rewrote the guard — so it is recorded rather than silently fixed in someone else's file.",
  "_rules": "Every entry here is PRINTED on every run. Adding an entry is a deliberate, reviewable act. An entry that stops reproducing FAILS the run until it is deleted, so an exemption can never outlive the defect it covers.",
  "_evidence": "docs/impl/laneD-test-infra.md, section 'TASK 2 — the real defect the guard found'",
  "known": {
    "supabase/functions/send-chat-message/index.ts:buildCallContext": "Declared ONLY inside the block comment spanning lines 69-328 ('BEGIN/END LEGACY CODE'), but CALLED at :498 in live code. Latent, not live: the call sits in the else-branch of `if (USE_SHARED_CONTEXT)` and USE_SHARED_CONTEXT is a const = true (:7), so the branch is unreachable today. It breaks the documented emergency-rollback path — flipping the flag to false throws ReferenceError instead of rolling back. Owner: whoever owns send-chat-message; not Lane D's file to edit."
  }
}
