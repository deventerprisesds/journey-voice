name: Check Scheduling Prefs

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Optional note'
        required: false
        default: ''

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      SB_URL: https://wwxgajrtmslzklnyplah.supabase.co
      SB_KEY: ${{ secrets.CF_SUPABASE_SERVICE_KEY }}
    steps:
      - name: Query user_scheduling_prefs scheduled_calls
        run: |
          python3 -c "
import json, urllib.request, urllib.parse, urllib.error, os

SB_URL = os.environ['SB_URL']
SB_KEY = os.environ['SB_KEY']

def get(path, params):
    url = f'{SB_URL}{path}?' + urllib.parse.urlencode(params)
    req = urllib.request.Request(url, headers={
        'apikey': SB_KEY, 'Authorization': f'Bearer {SB_KEY}', 'Accept': 'application/json'
    })
    try:
        with urllib.request.urlopen(req) as r:
            return json.loads(r.read()), r.status
    except urllib.error.HTTPError as e:
        return json.loads(e.read()), e.code

print('=== user_scheduling_prefs.scheduled_calls ===')
rows, code = get('/rest/v1/user_scheduling_prefs', {'select': 'user_id,timezone,scheduled_calls'})
print(f'HTTP {code} — {len(rows) if isinstance(rows, list) else rows} row(s)')
if code != 200:
    print('ERROR:', rows)
    exit(1)
for row in rows:
    uid = row.get('user_id','?')[:8]
    tz = row.get('timezone','?')
    calls = row.get('scheduled_calls') or []
    print(f'\nuser_id={uid}... timezone={tz}')
    for c in calls:
        cid = c.get('id','?')
        name = c.get('name','?')
        enabled = c.get('enabled', False)
        mode = c.get('commsMode','?')
        time = c.get('time','?')
        print(f'  [{\"ON \" if enabled else \"OFF\"}] {cid:<20} commsMode={mode:<12} time={time}  name={name}')

print()
print('=== scheduled_notifications (pending window calls) ===')
rows2, code2 = get('/rest/v1/scheduled_notifications', {
    'select': 'id,user_id,notification_type,body,scheduled_for,delivered_at',
    'notification_type': 'eq.scheduled_call',
    'delivered_at': 'is.null',
    'order': 'scheduled_for.asc',
    'limit': '10'
})
print(f'HTTP {code2} — {len(rows2) if isinstance(rows2, list) else rows2} pending row(s)')
if code2 != 200:
    print('ERROR:', rows2)
    exit(1)
for r in rows2:
    body = r.get('body') or {}
    if isinstance(body, str):
        try: body = json.loads(body)
        except: pass
    call_id = body.get('call_id','?')
    comms = body.get('commsMode','?')
    sched = r.get('scheduled_for','?')[:16]
    print(f'  call_id={call_id:<20} commsMode={comms:<12} scheduled_for={sched}')
"
